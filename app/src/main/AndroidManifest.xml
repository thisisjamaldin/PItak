<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nextinnovation.pitak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".register.CodeAuthenticationActivity" />
        <activity android:name=".register.RegisterClientActivity" />
        <activity android:name=".register.RegisterDriverActivity" />
        <activity android:name=".register.PhoneAuthenticationActivity" />
        <activity android:name=".register.WhoRegisterActivity" />
        <activity android:name=".start.OnBoardActivity" />
        <activity android:name=".main.MainActivity" />
        <activity
            android:name=".start.SplashActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>